# Local development only. Use the deployment provider's secret store in
# production and never commit a populated .env file.
SECRET_KEY=replace-with-a-long-random-development-secret
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1
